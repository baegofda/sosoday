import { SafeAreaView, View } from 'react-native';
import Font from 'src/components/Font';

const Home = () => {
  return (
    <SafeAreaView>
      <View>
        <Font>Home</Font>
      </View>
    </SafeAreaView>
  );
};

export default Home;
